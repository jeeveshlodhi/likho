# App
PROJECT_NAME=Likho Backend
API_V1_STR=/api/v1

# Database – SQLite for local dev; use PostgreSQL for Alembic migrations / production
DATABASE_URL=sqlite:///./sql_app.db
# DATABASE_URL=postgresql://user:pass@localhost/likho

# CORS – comma-separated list of allowed origins (e.g. frontend URLs)
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:5173
