# Local development — Vite dev server proxies /api → localhost:8000
# Leave VITE_API_BASE unset to use the proxy (see vite.config.ts server.proxy)
VITE_API_BASE=http://localhost:8000/api/v1
VITE_WS_BASE=ws://localhost:8000
